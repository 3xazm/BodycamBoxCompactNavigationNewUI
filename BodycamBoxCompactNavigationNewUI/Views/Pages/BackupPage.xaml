<Page
    x:Class="BodycamBoxCompactNavigationNewUI.Views.Pages.BackupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid>
        <StackPanel Margin="40" Orientation="Vertical" >

            <!-- 标题 -->
            <TextBlock Text="Bodycam 玩家数据保存"
               FontSize="24"
               FontWeight="SemiBold"
               Foreground="#a4c9cc"
               Margin="0,0,0,20"/>

            <!-- 卡片 -->
            <Border CornerRadius="16"
            Padding="20"
            Background="{DynamicResource ControlFillColorDefaultBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- 左边：保存 -->
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">

                        <TextBlock Text="把玩家数据保存/备份在："
                           FontSize="14"
                           Margin="0,0,0,5"/>

                        <TextBlock Text="请选择要放入备份的路径："
                           FontSize="12"
                           Opacity="0.7"
                           Margin="0,0,0,5"/>   

                        <ComboBox x:Name="DriveBox1"
                          Height="35"
                          Margin="0,0,0,10"/>

                        <Button Content="保存/备份"
                        Height="35"
                        Click="Save_Click"/>
                    </StackPanel>

                    <!-- 右边：恢复 -->
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">

                        <TextBlock Text="把玩家数据写入新设备："
                           FontSize="14"
                           Margin="0,0,0,5"/>

                        <TextBlock Text="请选择带有备份数据的路径："
                           FontSize="12"
                           Opacity="0.7"
                           Margin="0,0,0,5"/>

                        <ComboBox x:Name="DriveBox2"
                          Height="35"
                          Margin="0,0,0,10"/>

                        <Button Content="写入新设备"
                        Height="35"
                        Click="Load_Click"/>
                    </StackPanel>

                </Grid>
            </Border>

        </StackPanel>
    </Grid>
</Page>